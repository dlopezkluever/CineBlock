Allow fo differnet shapes. (dog/cat + rod)


Allow a auto framer postioning of camera tools
Or at least a match cut tool that returns the camera where it needs to be 
--> shot 2A is a shot pointed at the 

if you have shot locations, and then the world model is of the location directions, (left right = 2nd wall, 4th wall) then the camera automatically points there

Or that a medium and clsoe shot (with Hero in it specified in prompt) then the camera can automatically move to be close up from where the character is 

Like easy for an agent to move

all you do is place the characters where they are in the room, and the camera can automatically move into position

Gotta link with the pipeline